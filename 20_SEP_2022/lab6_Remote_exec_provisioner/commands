
#create a key pair

ssh-keygen -t rsa
cat /home/ec2-user/.ssh/id_rsa.pub
terraform validate
terraform apply
ssh ec2-user@44.204.191.225

# After creating a key pair import the public key into the AWS console, which will be used to create a server.